# Historical classical pipeline

This directory preserves the classical tracker, heuristic stitcher,
lineage logic, reconciliation logic, visualization utilities, and old
numbered orchestration that preceded the current learned architecture.

**Active production packages must not import from this directory.**

`notebooks/`, `investigations/`, and `experiments/` are intentionally left
unchanged as the proof-of-path record of how the architecture evolved.
