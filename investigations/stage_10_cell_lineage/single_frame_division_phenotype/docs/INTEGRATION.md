# Integration

Extract this ZIP into the repository root. It adds only:

```text
investigations/stage_10_cell_lineage/single_frame_division_phenotype/
tests/test_single_frame_division_phenotype.py
```

It does not replace or edit existing repository files.

Run from the repository root with the existing project virtual environment activated:

```powershell
python -m investigations.stage_10_cell_lineage.single_frame_division_phenotype.run_analysis
```
